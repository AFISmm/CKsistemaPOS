# dependencias
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# produccion
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo

.vercel
.env*

# material fuente (grabaciones/transcripciones de reuniones) — nunca al repo,
# demasiado grande y no es un artefacto de proyecto; el analisis extraido SI
# se versiona en docs/analisis-*.md
*.mp4
POS.docx
Llamada con Revisión POS.docx
Matriz Funcional POS.xlsx
Recetario_Simplificado.xlsx
# entregables generados (Word/PDF) para el usuario — no son fuente del
# proyecto, el contenido versionado equivalente vive en docs/*.md
Revision-POS-*.docx
